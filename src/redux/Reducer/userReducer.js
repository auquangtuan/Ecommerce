
let user = {}

const stateDefault = {
    userLogin: user
}


export const userReducer = ( state = stateDefault, action) => {
    switch (action.type) {
        
    
        default:
            return {...state}
    }
}